<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Lease Renewal - {{brandName}}</title>
    <style
    >
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        margin: 0;
        padding: 0;
        background-color: #f9f9f9;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 5px;
        overflow: hidden;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .header {
        background-color: {{brandColor}};
        color: white;
        padding: 20px;
        text-align: center;
      }
      .content {
        padding: 20px;
      }
      .button {
        display: inline-block;
        padding: 10px 20px;
        background-color: {{brandColor}};
        color: white;
        text-decoration: none;
        border-radius: 4px;
        margin: 15px 0;
      }
      .info-box {
        background-color: #f8f9fa;
        padding: 15px;
        border: 1px solid #e9ecef;
        border-radius: 5px;
        margin: 20px 0;
      }
      .footer {
        background-color: #f5f5f5;
        padding: 15px;
        text-align: center;
        font-size: 12px;
        color: #666;
      }
      .property-details {
        margin-top: 10px;
      }
      .detail-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 8px;
        border-bottom: 1px solid #eee;
        padding-bottom: 8px;
      }
      .detail-label {
        font-weight: bold;
        color: #555;
      }
      .action-required {
        background-color: #fff3cd;
        padding: 10px;
        border-left: 4px solid #ffc107;
        margin: 15px 0;
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        {{#if brandLogoUrl}}
          <div style='text-align:center;margin-bottom:12px;'>
            <img src='{{brandLogoUrl}}' alt='{{brandName}}' style='max-height:150px;' />
          </div>
        {{/if}}
        <h1>Lease Renewal {{#if isAutoRenewal}}Notice{{else}}Opportunity{{/if}}</h1>
      </div>
      <div class='content'>
        <h2>Hello {{capitalize recipientName}}!</h2>

        {{#if isAutoRenewal}}
          <div class='action-required'>
            <h3>Auto-Renewal Notice</h3>
            <p>Your lease will be automatically renewed on
              {{formatDate renewalDate}}
              according to your lease agreement.</p>
          </div>
          <p>Your lease for the following property will be automatically renewed:</p>
        {{else}}
          <div class='action-required'>
            <h3>Action Required: Lease Renewal</h3>
          </div>
          <p>We'd like to offer you the opportunity to renew your lease for the following property:</p>
        {{/if}}

        <div class='info-box'>
          <h3>Lease Details</h3>
          <div class='property-details'>
            <div class='detail-row'>
              <span class='detail-label'>Property:</span>
              <span>{{propertyName}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Unit:</span>
              <span>{{unitIdentifier}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Current Lease End:</span>
              <span>{{formatDate currentLeaseEndDate}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>New Lease Start:</span>
              <span>{{formatDate newLeaseStartDate}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>New Lease End:</span>
              <span>{{formatDate newLeaseEndDate}}</span>
            </div>
            {{#if newMonthlyRent}}
              <div class='detail-row'>
                <span class='detail-label'>Current Monthly Rent:</span>
                <span>${{currentMonthlyRent}}</span>
              </div>
              <div class='detail-row'>
                <span class='detail-label'>New Monthly Rent:</span>
                <span>${{newMonthlyRent}}</span>
              </div>
            {{else}}
              <div class='detail-row'>
                <span class='detail-label'>Monthly Rent:</span>
                <span>${{currentMonthlyRent}}</span>
              </div>
            {{/if}}
          </div>
        </div>

        {{#if isAutoRenewal}}
          <p>If you wish to opt out of the automatic renewal, please contact your property manager
            as soon as possible.</p>
        {{else}}
          <p>To accept this renewal offer, please log in to your account and follow the renewal
            process, or contact your property manager directly.</p>
        {{/if}}

        <p>If you have any questions about your lease renewal, please don't hesitate to contact us.</p>
      </div>
      <div class='footer'>
        <p>Best regards,<br />The {{brandName}} Team</p>
        <p>© {{currentYear}} {{brandName}}. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
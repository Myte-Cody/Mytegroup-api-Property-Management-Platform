<html>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Lease Activated - Property Management Platform</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #28a745;
        color: white;
        padding: 20px;
        text-align: center;
      }
      .content {
        padding: 20px;
        background-color: #f9f9f9;
      }
      .button {
        display: inline-block;
        padding: 10px 20px;
        background-color: #28a745;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        margin: 15px 0;
      }
      .info-box {
        background-color: #e9f7ef;
        padding: 15px;
        border: 1px solid #d5f5e3;
        border-radius: 5px;
        margin: 15px 0;
      }
      .footer {
        text-align: center;
        padding: 20px;
        font-size: 12px;
        color: #666;
      }
      .property-details {
        margin-top: 20px;
        border-top: 1px solid #ddd;
        padding-top: 15px;
      }
      .detail-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 5px;
      }
      .detail-label {
        font-weight: bold;
        color: #555;
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        <h1>Lease Activated</h1>
      </div>
      <div class='content'>
        <h2>Hello {{capitalize recipientName}}!</h2>

        {{#if isTenant}}
          <p>Your lease for the following property has been activated:</p>
        {{else}}
          <p>A lease for the following property has been activated:</p>
        {{/if}}

        <div class='info-box'>
          <h3>Lease Details</h3>
          <div class='property-details'>
            <div class='detail-row'>
              <span class='detail-label'>Property:</span>
              <span>{{propertyName}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Unit:</span>
              <span>{{unitIdentifier}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Address:</span>
              <span>{{formatAddress propertyAddress}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Lease Start:</span>
              <span>{{formatDate leaseStartDate}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Lease End:</span>
              <span>{{formatDate leaseEndDate}}</span>
            </div>
            <div class='detail-row'>
              <span class='detail-label'>Monthly Rent:</span>
              <span>${{monthlyRent}}</span>
            </div>
          </div>
        </div>

        {{#if isTenant}}
          <p>Welcome to your new home! Your lease is now active and you can access all tenant
            features on our platform.</p>
        {{else}}
          <p>The lease has been successfully activated and all parties have been notified.</p>
        {{/if}}

        {{#if dashboardUrl}}
          <p>View complete lease details on your dashboard:</p>
          <p><a href='{{dashboardUrl}}' class='button'>Go to Dashboard</a></p>
        {{/if}}

        <p>If you have any questions about your lease, please contact your property manager.</p>
      </div>
      <div class='footer'>
        <p>Best regards,<br />The Property Management Team</p>
        <p>© 2025 Property Management Platform. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>